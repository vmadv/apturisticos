services:
  - type: web
    name: apartamentos-turisticos-sevilla
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: CRON_SECRET
        generateValue: true
